# .env
# PostgreSQL connection (adjust if you set a password later)
# For now, since Homebrew uses trust auth → no password needed
DATABASE_URL=postgresql://localhost/attendance_db

# If you ever set a username/password (recommended for production):
# DATABASE_URL=postgresql://yourmacusername:yourpassword@localhost:5432/attendance_db

# Other future creds (examples)
#SECRET_KEY=super-secret-change-me-123456
DEBUG=true
APP_NAME='Employee Attendance API'